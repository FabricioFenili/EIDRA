ALERT_RULES = {
    "freshness_breach": "trigger when dataset freshness exceeds threshold",
    "execution_cost_spike": "trigger when implementation shortfall exceeds threshold",
    "alpha_decay": "trigger when rolling IC and rolling Sharpe deteriorate materially",
}
