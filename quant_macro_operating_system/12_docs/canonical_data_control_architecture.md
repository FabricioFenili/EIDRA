# Canonical Data Control Architecture

All sources differ only at capture. After capture, every dataset enters the same institutional chain:

source_container
-> source_adapter
-> canonical_contract
-> ingestion
-> normalization
-> validation
-> curation
-> publish
-> platinum
